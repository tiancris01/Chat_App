import 'package:flutter/material.dart';

class preloadingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('preloading'),
      ),
    );
  }
}
